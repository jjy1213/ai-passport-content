# AI Passport 课程规划

## 当前四个主题

`courses/v2/` 中的早安时光、动物朋友、点心时间、故事旅行各有 20 个不同的词。每张词卡包含英文单词、中文释义、英文例句和例句中文翻译。课程文件使用带版本的路径；旧版 `courses/*-course.json` 保留，避免已下载的链接失效。

首批 160 段英文语音素材已生成：每张词卡各有单词朗读和例句朗读，存于 `audio/v3/`，v3 课程分别填入 `wordAudio` 和 `exampleAudio`。网页应明确标识合成语音。没有音频地址时不显示播放操作，也不播放错误的离线录音。设备真机播放仍需验收。

语音和文本审校统一使用阿里云百炼千问。仅在本机的 `DASHSCOPE_API_KEY` 环境变量保存按量计费的百炼 API Key，不要写入仓库或聊天；所选密钥的地域与 DashScope 接口地域必须一致。运行脚本需 Python 3.11 和 `dashscope` 包。先运行 `tools/validate_courses.py`、`tools/review_qwen_courses.py`，人工复核报告后，再运行 `tools/generate_qwen_voice_pack.py --dry-run` 及正式生成。生成完再次校验并测试网页与设备播放；语音未通过验收前不要发布新版本。

## 旧词库规模

2026-09-24 对设备原有 `main/english_course.c` 的静态统计：共有 3,020 行，去重后为 1,502 个英文词。其中 2,964 行释义仍为“常用词”，约 3,000 行例句是 `Say ... aloud.` 占位文本。该列表不能直接作为儿童课程发布，必须人工复核词义、词性、年级适配、例句和翻译。

新四个主题有 80 个不同词，其中 21 个出现在旧词库中。若保留旧词库的每个不同词，合并后是 1,561 个不同词，按 20 词一组需要 `ceil(1561 / 20) = 79` 个模块：当前 4 个主题之外还需约 75 个。剔除重复、错误、不适龄或无教学价值的词后，最终模块数会减少。

## 后续模块规则

1. 先审核词表，再按年级和生活场景分组。一个模块最多 20 个目标词；不要为凑数塞进无关词。
2. 每个词保留明确的教学义项；英文例句自然地使用该词，中文翻译与英文例句语义一致。
3. 先完成文本审校，再批量生成英文单词及例句语音；音频文件和课程路径都带版本，避免旧设备缓存拿到不匹配内容。
4. 目录为每个课程声明字节数与 SHA-256；设备下载后校验，通过才显示课程。设备只保留正在使用的课程数据与小块播放缓冲。
5. 课程失效、网络中断或校验失败时展示清晰提示；可返回设备内的少量离线入门内容。

## v2 JSON 字段

`themes.json` 提供 `id`、双语标题、`version`、`itemCount`、`download`、`estimatedBytes` 和 `sha256`。课程文件提供 `format`、`schemaVersion`、`theme`、双语标题、`version` 和 `items`。每个 item 必须有 `word`、`meaningZh`、`example`、`exampleZh`；以后增加可选的 `wordAudio` 和 `exampleAudio` 相对路径。

设备从同源的 `device-catalog.json` 读取精简目录。发布脚本必须检查它与 `themes.json` 的课程路径和 SHA-256 一致，新增模块时同步更新两份目录。
